<Window x:Class="Minesweeper.GameEasy"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
        xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
        xmlns:local="clr-namespace:Minesweeper"
        mc:Ignorable="d"
        Title="Minesweeper" Height="600" Width="400"
        Background="#C0C0C0"
        FontFamily="Digital-7 Regular"
        ResizeMode="NoResize">

    <Window.Resources>
        <local:BoardVisualConverter x:Key="BoardVisualConverter" />
    </Window.Resources>

    <Grid Width="350px" Height="550px" VerticalAlignment="Bottom" Margin="0,0,0,25">
        <TextBlock />
        <ItemsControl Name="gameBoardArray" Height="350px" VerticalAlignment="Bottom">
            <ItemsControl.ItemsPanel>
                <ItemsPanelTemplate>
                    <UniformGrid Columns="8" Rows="8"/>
                </ItemsPanelTemplate>
            </ItemsControl.ItemsPanel>
            <ItemsControl.ItemTemplate>
                <DataTemplate>
                    <Border BorderBrush="White" BorderThickness="1" Background="White">
                        <Grid>
                            <Button x:Name="myButton" Click="Button_Click"  Background="Silver"/>
                            <TextBlock  Text="{Binding}" 
                                        VerticalAlignment="Center" 
                                        HorizontalAlignment="Center" 
                                        FontSize="24"
                                        Foreground="{Binding Converter={StaticResource BoardVisualConverter}}"
                                        Background="White" 
                                        Visibility="Hidden"/>
                        </Grid>
                    </Border>
                </DataTemplate>
            </ItemsControl.ItemTemplate>
        </ItemsControl>
    </Grid>
</Window>
